"""output_filter — security guard skeleton (input/content/output layers).
Implement checks here; the agent loop calls guards at the boundary so policy
lives in one place. Left as a stub deliberately — design these against your
actual threat model, not generic defaults."""
